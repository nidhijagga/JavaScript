// Remember, we're gonna use strict mode in all scripts now!
'use strict';

//Installing Prettier Extention and set as default formattor of VS Code.
const x = 89;
if (x === 90) console.log(x);

const calAge = (birthYear) => 2023 - 1998;

console.log(calAge);
